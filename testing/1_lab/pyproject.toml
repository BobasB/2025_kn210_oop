[project]
name = "1-lab"
version = "0.1.0"
description = "Проект для тестування програм"
authors = [
    {name = "BobasB",email = "bugil.bogdan@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[dependency-groups]
dev = [
    "black (>=26.5.1,<27.0.0)"
]

[tool.poetry]
packages = [
    { include = "lab_main", from = "src" }
]



[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
